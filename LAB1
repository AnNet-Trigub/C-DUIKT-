#include <iostream>
#include <cmath>

int main() {
    double a = 4.0;
    double b = 0.5;
    double y;

    y = (0.75 * sqrt(b) - 0.5 * cbrt(a)) * sin(b * M_PI / 3);

    std::cout << "y = " << y << std::endl;

    return 0;
}
